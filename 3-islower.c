#include "main.h"

/**
 * _islower - checks for a lower case letter
 *
 * @c : character to be checked if lower case or not
 * Return: 0 or 1
 *
 */

int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
